/* Importamos la funcion al archivo principal */
import verificarEmail from "./function.js";
/* Enviamos el parametro email*/
let email = ("hola");
let resultado = verificarEmail(email); 
console.log(resultado);